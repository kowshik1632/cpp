#include <iostream>
using namespace std;

int main() 
{
  double a,b,product;
  cout<<"Enter two numbers:\n ";
  cin >> a >> b;
  product=a*b;
  cout<<"Product of given numbers= "<<product;
  
  return 0;
}
